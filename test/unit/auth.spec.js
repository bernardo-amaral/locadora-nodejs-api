const { expect } = require('chai');

describe('Auth Tests', () => {
  describe('#login', () => {
    it('Shoult try login with an user and return success', () => {
      expect(true);
    });
  });
});
